nav.navbar.navbar-default
	.d-flex.justify-content-between.align-items-center
		a.ml-3.logo.navbar-brand(href="/")
			h2 Agent
		form.form-inline(action="/login", method="post")
			.form-group
				input.mr-3(type="text", name="username", placeholder="Username" required)
			.form-group
				input.mr-3(type="password", name="password", placeholder="Password" required)
			input.mr-3.btn.btn-primary(type="submit", value="Login")
