function onlyFaculty(req, res, next) {
  /* Middleware ensures a user is a faculty */
  if (req.session.status === 'faculty') {
    return next();
  }
  req.flash('error', 'Have to be a faculty');
  return res.redirect('/');
}

module.exports = onlyFaculty;
